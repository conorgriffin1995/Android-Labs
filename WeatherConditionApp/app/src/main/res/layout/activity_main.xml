<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <Spinner
        android:id="@+id/citySpinner"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp" />

    <TextView
        android:id="@+id/temperatureTextView"
        android:layout_width="75dp"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/conditionsImageView"
        android:layout_alignRight="@+id/conditionsImageView"
        android:layout_below="@+id/citySpinner"
        android:layout_marginTop="18dp" />

    <ImageView
        android:id="@+id/conditionsImageView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignLeft="@+id/citySpinner"
        android:layout_alignStart="@+id/citySpinner"
        android:layout_below="@+id/temperatureTextView"
        android:layout_marginTop="19dp"
        android:contentDescription="@string/conditionsImageViewContentDescription"
        android:src="@mipmap/ic_launcher" />

</RelativeLayout>
